# Core ML
ultralytics>=8.0.100

# Torch (CPU only)
torch==2.3.0+cpu
torchvision==0.18.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Computer Vision (headless reduces image size)
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0

# Configuration
PyYAML>=6.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# CLI
click>=8.1.0
rich>=13.0.0

# Logging
loguru>=0.7.0
tqdm>=4.65.0

# API
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# Visualization (these are heavy — keep only if needed)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0
tensorboard>=2.14.0

# Development (⚠️ NÃO instale isso na imagem final!)
# black>=23.0.0
# flake8>=6.0.0
# isort>=5.12.0
# pre-commit>=3.3.0
# pytest>=7.4.0
# pytest-cov>=4.1.0
